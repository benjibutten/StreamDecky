{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "StreamDecky (safe debug)",
      "type": "coreclr",
      "request": "launch",
      "preLaunchTask": "build StreamDecky (safe)",
      "program": "${workspaceFolder}/StreamDecky/bin/Debug/net10.0-windows/StreamDecky.exe",
      "cwd": "${workspaceFolder}/StreamDecky",
      "stopAtEntry": false,
      "console": "internalConsole"
    }
  ]
}